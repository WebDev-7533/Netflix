1. To install node_modules, run 'npm install' inside of netflix folder
2. To start the server, run 'npm start'
3. Open your web browser (chrome / firefox / safari) and enter 'localhost:3000' 



In Case of Error:

Delete node modules folder and package-lock-json file than again run above commands.
1) npm install
2) npm start
